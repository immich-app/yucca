---
# CI-MANAGED. The deploy workflow's gated `deploy-prod` job rewrites this to
# the SAME short git SHA already validated in staging (promotion of one
# artifact), behind the GitHub Environment approval. See staging's copy.
apiVersion: v1
kind: ConfigMap
metadata:
  name: image-versions
  namespace: flux-system
data:
  YUCCA_IMAGE_TAG: main
