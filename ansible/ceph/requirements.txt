ansible-core==2.20.7
ansible-lint==26.6.0
ansible-navigator>=25.0
yamllint==1.38.0
molecule>=25.0
boto3==1.42.87
# Required by the `password_hash` filter (baseline: ops user password). Python
# 3.13 dropped the `crypt` stdlib module ansible used to fall back on, so the
# filter now hard-requires passlib on the controller — without it the play
# fails: "Unable to encrypt nor hash, passlib must be installed".
passlib==1.7.4
