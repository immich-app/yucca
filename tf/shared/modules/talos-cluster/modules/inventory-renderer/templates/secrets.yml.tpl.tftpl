---
# Talos ${cluster_name} cluster secrets template.
# TF-GENERATED — do not edit by hand. Source: tf/shared/modules/talos-cluster/modules/inventory-renderer/templates/secrets.yml.tpl.tftpl
#
# This template is intentionally empty (no cluster-level secrets needed
# yet). scripts/ansible-play.sh detects the file presence; an empty (but
# valid YAML) file would still trigger op inject and a 1P session
# requirement. So we render a placeholder treated as "no secrets needed"
# by way of NOT being created at all — see main.tf for the conditional.
#
# Populate with op:// references for any cluster-level secrets that
# emerge (e.g. talosconfig for storage, etc).

# No secrets yet.
