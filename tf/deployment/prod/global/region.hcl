# Reserved `global` pseudo-region — account-wide prod stacks (NetBird). role null.
locals {
  role          = null
  site_id       = null
  datacenter    = null
  provider_code = null
  domain        = null
}
