# Reserved `global` pseudo-region — partition-wide stacks (DNS). role/FQDN null.
locals {
  role          = null
  site_id       = null
  datacenter    = null
  provider_code = null
  domain        = null
}
